# ====================================================
# Costs Dashboard Demo Mode (for UI development)
# ====================================================
# Enable this profile with: -Dspring.profiles.active=dev,costs-demo

# Enable GCP billing (will use mock data)
gcp.billing.enabled=true
gcp.billing.demo-mode=true

# Mock data will be generated by the services
# No actual GCP credentials needed in demo mode
