<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
		 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>io.strategiz</groupId>
		<artifactId>service</artifactId>
		<version>1.0-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>

	<artifactId>service-console-accessibility</artifactId>
	<packaging>jar</packaging>

	<name>Service - Console Accessibility</name>
	<description>Accessibility metrics and WCAG compliance for admin console</description>

	<dependencies>
		<!-- Service Base -->
		<dependency>
			<groupId>io.strategiz</groupId>
			<artifactId>service-framework-base</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- Data - Accessibility (for cached metrics entities and repository interface) -->
		<dependency>
			<groupId>io.strategiz</groupId>
			<artifactId>data-accessibility</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- Client - Firebase (for Firestore implementation of accessibility cache repository) -->
		<dependency>
			<groupId>io.strategiz</groupId>
			<artifactId>client-firebase</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- Spring Boot Web -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<!-- Validation -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!-- Spring Vault (for CI/CD token authentication) -->
		<dependency>
			<groupId>org.springframework.vault</groupId>
			<artifactId>spring-vault-core</artifactId>
			<version>3.2.0</version>
		</dependency>

		<!-- JWT for GitHub App authentication -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.12.3</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.12.3</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.12.3</version>
			<scope>runtime</scope>
		</dependency>

		<!-- Test Dependencies -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

</project>
