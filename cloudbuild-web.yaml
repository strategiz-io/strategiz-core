steps:
# Build the application with Maven
- name: 'maven:3.9.6-eclipse-temurin-21'
  entrypoint: 'mvn'
  args: ['clean', 'package', '-DskipTests', '-pl', 'application-web', '-am']

# Build the Docker image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-f', 'application-web/Dockerfile', '-t', 'gcr.io/$PROJECT_ID/strategiz-web:$BUILD_ID', '-t', 'gcr.io/$PROJECT_ID/strategiz-web:latest', './application-web']

# Push the Docker image
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/strategiz-web:$BUILD_ID']

- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/strategiz-web:latest']

# Deploy to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'gcloud'
  args:
    - 'run'
    - 'deploy'
    - 'strategiz-web'
    - '--image=gcr.io/$PROJECT_ID/strategiz-web:$BUILD_ID'
    - '--platform=managed'
    - '--region=us-central1'
    - '--allow-unauthenticated'
    - '--memory=1Gi'
    - '--cpu=1'
    - '--port=8080'
    - '--min-instances=0'
    - '--max-instances=10'
    - '--set-env-vars=SPRING_PROFILES_ACTIVE=prod'
    - '--set-env-vars=VAULT_ADDR=https://strategiz-vault-43628135674.us-central1.run.app'
    - '--set-env-vars=STRATEGIZ_VAULT_ADDRESS=https://strategiz-vault-43628135674.us-central1.run.app'
    - '--set-secrets=VAULT_TOKEN=vault-root-token:latest'

images:
- 'gcr.io/$PROJECT_ID/strategiz-web:$BUILD_ID'
- 'gcr.io/$PROJECT_ID/strategiz-web:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'

timeout: '1800s'
